test:
  override:
    - node packager.js

deployment:
  production:
    branch: master
    commands:
      - git config --global user.email onevasari@gmail.com
      - git config --global user.name 1vasari
      - git status
      - git add songs.json && git commit -m "[CircleCI][ci skip] build songs.json"
      - git push origin master
